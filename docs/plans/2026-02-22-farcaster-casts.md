# design-loop Farcaster Casts

3 standalone casts over 6 days. NOT a thread — Farcaster culture favors strong single casts.

**Timing:** Post between 10 AM – 12 PM EST (midnight–2 AM KST). Farcaster algorithm rewards early engagement heavily.

---

## Cast 1 — The Hook (Day 2, 12-24 hrs after X thread)

**ATTACH:** Demo site video from X Tweet 1 (square crop, 1080x1080).

```
AI can code your frontend. But the output always looks... AI-generated.

Built a Claude Code plugin that takes screenshots of what Claude builds,
scores it against 5 design criteria, and fixes it autonomously until it
looks designed, not generated.

design-loop.vercel.app — click through the iterations

One command: claude plugin add https://github.com/tonymfer/design-loop
```

**Why this works for Farcaster:** One cast. Problem → solution → proof → action. No thread needed. The crypto-builder audience respects density. The demo site URL lets anyone verify immediately.

---

## Cast 2 — Composable Intelligence (Day 4-5)

```
The part of design-loop that surprises people:

It discovers other design skills you've installed and absorbs their
guidelines at runtime. Zero config.

Install frontend-design → design-loop gets bolder aesthetic principles
Install web-design-guidelines → it layers WCAG compliance

The plugin ecosystem as composable intelligence.
```

**Why "composable" matters here:** Farcaster audience understands composable protocols (DeFi legos). "Composable intelligence" maps design-loop onto a mental model they already value. This is the cast that gets crypto-native builders interested.

---

## Cast 3 — Builder Lessons (Day 6+, only if engagement warrants)

```
What I learned building design-loop (and shipping it as a solo builder):

1. The product is a SKILL.md file. No runtime code. The AI reads
   instructions and executes them. The "code" is prose.
2. A stop hook (bash script) turns a one-shot AI into an autonomous loop
3. The demo site was polished by the tool itself

If you're building AI tools, the leverage is insane. The hard part is
the design taste, not the engineering.
```

**Why this matters for career:** "The product is a markdown file" is genuinely surprising. This is the cast that gets you founder DMs. It shows you understand how AI products actually work in 2026.

---

## Notes

- Do NOT build a Farcaster Frame for launch. The demo site already serves the purpose (interactive, clickable)
- Consider a "score your site" Frame as a follow-up if traction happens — that could be a separate viral moment
- Only post Cast 3 if Cast 1-2 got meaningful engagement (10+ recasts or 50+ likes)
